yfinance>=0.2.31
lightgbm>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
optuna>=3.4.0
lxml>=4.9.0
alpaca-py>=0.21.0
httpx>=0.24.0
torch>=2.0.0
hmmlearn>=0.3.0
# Optional: mamba-ssm for 10-50x faster CrossMamba on GPU
# pip install mamba-ssm causal-conv1d
# Only works on Linux with CUDA. Falls back to Python scan without it.
